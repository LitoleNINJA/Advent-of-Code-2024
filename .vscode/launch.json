{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch All Go Files",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {},
            "buildFlags": "-v"
        }
    ]
}